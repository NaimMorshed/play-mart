import React from 'react';

const Root = () => {
    return (
        <div className="App">
            <h1>Administrator Panel</h1>
            <h5>This page is only accessible for administrator only.</h5>
            <p className="blur-text">Navigate to side drawer to access admin panel contents.</p>
        </div>
    );
};

export default Root;